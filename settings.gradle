rootProject.name = 'ab-platform'

include(
        "app",
        "shared:common",
        "shared:caching",
        "shared:contracts",
        "shared:locking",
        "shared:testing",
        "modules:feature-flag",
        "modules:experiment",
        "modules:assignment",
)
