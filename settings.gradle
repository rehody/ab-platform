rootProject.name = 'ab-platform'

include(
        "app",
        "shared:common",
        "shared:contracts",
        "shared:locking",
        "shared:testing",
        "modules:feature-flag",
        "modules:experiment",
)
