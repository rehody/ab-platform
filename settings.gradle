rootProject.name = 'ab-platform'

include(
        "app",
        "shared:common",
        "shared:contracts",
        "shared:locking",
        "modules:feature-flag",
        "modules:experiment",
)
